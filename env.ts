import type { EnvOptions } from 'tnp/src';

const env: Partial<EnvOptions> = {
  website: {
    domain: 'typescript-class-helpers.example.domain.com',
    title: 'Typescript Class Helpers',
  },
};
export default env;
